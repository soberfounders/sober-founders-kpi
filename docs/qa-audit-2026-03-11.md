# QA Audit — KPI Dashboard Fixes
**Date:** 2026-03-11
**Auditor:** Claude (Sonnet 4.6) — pass complete, verdict APPROVED
**For:** Next agent performing independent verification

## Commits Under Review
```
d2e38d9  chore: add CLAUDE.md project instructions
4c6f34a  fix(dashboard): correct trend arrow, interview matcher, enrichment gate, and race condition
8482d6c  fix(dashboard): correct cost-metric trend arrow and unify KPICard tone colors
8b0a64c  fix(helpers): return N/A for non-finite values in formatInt and formatDecimal
```

All four commits are on `main` and pushed to `origin/main`.

---

## Files Changed (scope of this audit)

| File | Commits |
|------|---------|
| `dashboard/src/lib/dashboardKpiHelpers.js` | 8b0a64c |
| `dashboard/src/components/KPICard.jsx` | 8482d6c |
| `dashboard/src/views/DashboardOverview.jsx` | 8482d6c, 4c6f34a |
| `dashboard/src/views/LeadsDashboard.jsx` | 4c6f34a |
| `dashboard/src/views/AttendanceDashboard.jsx` | 4c6f34a |
| `dashboard/tests/dashboardKpiHelpers.test.mjs` | 4c6f34a |
| `agents.md` | 8b0a64c |
| `CLAUDE.md` | d2e38d9 |

---

## How to Run This Audit

For every check below: read the referenced file at the referenced line, confirm the assertion, and record PASS / FAIL / WARNING.

---

## CHECK GROUP A — formatInt / formatDecimal (dashboardKpiHelpers.js)

**A1.** Read `dashboard/src/lib/dashboardKpiHelpers.js` lines 47–50.
Assert: `formatInt` first line is `if (!Number.isFinite(Number(value))) return 'N/A';`

**A2.** Read lines 52–55.
Assert: `formatDecimal` first line is `if (!Number.isFinite(Number(value))) return 'N/A';`

**A3.** Read lines 57–70.
Assert: Both `formatCurrency` (line 58) and `formatPercent` (line 68) have the same `'N/A'` guard — these must not have regressed.

**A4.** Read lines 93–108 (`formatSignedDelta`).
Assert: Line 95 is `if (!Number.isFinite(numeric)) return 'N/A';` — this guard fires BEFORE any call to `formatDecimal` or `formatInt` inside the function. Confirm the guard line number.

**A5.** Read lines 72–77 (`formatValueByType`).
Assert: The default (non-currency/decimal/percent) branch calls `formatInt(value)` — meaning non-finite counts now return `'N/A'` not `'0'`.

**A6.** Search `dashboard/src/views/WebsiteTrafficDashboard.jsx` for:
- `function formatInt` — assert it exists locally (around line 77)
- `import.*dashboardKpiHelpers` — assert this import does NOT exist
Both must be true for the file to be unaffected by the change.

---

## CHECK GROUP B — agents.md correctness

**B1.** Read `agents.md`. Assert a section "Formatting Functions — Behavior Contract" exists and lists `formatInt`, `formatDecimal`, `formatCurrency`, `formatPercent` as all returning `'N/A'` for non-finite inputs. Assert the rule "Never add a fallback of '0' for missing data" is present.

**B2.** Assert `agents.md` contains two clearly separated classification systems:
- System 1 (Qualified/Unqualified): requires revenue >= $250,000 AND sobriety strictly > 1 year
- System 2 (Revenue Tier): Bad/OK/Good/Great based on revenue only — sobriety is explicitly irrelevant

**B3.** Assert `agents.md` attendance section names exactly two sources: Luma (Thursday sessions) and HubSpot Meeting Activities (Tuesday + Thursday). Assert Zoom is listed under "Deprecated — Do Not Reference" with specific meeting IDs `87199667045` and `84242212480`.

**B4.** Assert `agents.md` Open Issues section contains all three of:
- `KPICard` `backgroundColor: 'white'` hardcoded
- `buildMustDoToday` Leads weight asymmetry
- Missing unit tests for `leadsQualificationRules.js`, `leadsConfidenceModel.js`, `buildCardModel`

---

## CHECK GROUP C — KPICard TONE constant (KPICard.jsx)

**C1.** Read `dashboard/src/components/KPICard.jsx` lines 1–15.
Assert: A module-level `const TONE` object exists with exactly three keys: `better`, `worse`, `neutral`. Each must have both `text` and `bg` string properties. Assert `TONE.neutral.text` is `'#64748b'` (NOT the old `'#334155'`).

**C2.** Grep `KPICard.jsx` for each hex. Assert the following are ABSENT from color-logic lines (acceptable only inside the TONE constant definition or the intentional comment):
- `#10b981` — old trend badge better color → must be ABSENT
- `#ef4444` — old trend badge worse color → must be ABSENT
- `#15803d` — old comparisonRows better → must be ABSENT
- `#b91c1c` — old comparisonRows worse → must be ABSENT

**C3.** Read line 34 (`previousToneColor`).
Assert: `(TONE[previousTone] ?? TONE.neutral).text` — the `??` fallback must use `??` not `?.`.

**C4.** Read lines 74–75 (trend badge style).
Assert: Background uses `TONE.better.bg`, `TONE.worse.bg`, `TONE.neutral.bg`. Color uses `TONE.better.text`, `TONE.worse.text`, `TONE.neutral.text`. No inline hex codes.

**C5.** Read line 95 (comparisonRows toneColor).
Assert: `(TONE[row?.tone] ?? TONE.neutral).text` — safe access with `?.` on `row` and `??` fallback.

**C6.** Read lines 40–50 (backgroundColor).
Assert: `backgroundColor: 'white'` is present. Assert there is a multi-line comment immediately above it explaining the design rationale (light surface for dark text colors).

---

## CHECK GROUP D — buildCardModel trend arrow (DashboardOverview.jsx)

Search for `function buildCardModel` then read approximately 55 lines.

**D1.** Assert `rawDelta` is defined as:
```js
const rawDelta = Number.isFinite(Number(current)) && Number.isFinite(Number(previous))
  ? Number(current) - Number(previous)
  : null;
```
This uses period-over-period (current window vs prior window), NOT `lastWeekComparison.delta`.

**D2.** Assert `trend` is defined as:
```js
const trend = rawDelta === null ? 'neutral' : rawDelta > 0 ? 'up' : rawDelta < 0 ? 'down' : 'neutral';
```
NOT derived from `lastWeekComparison`.

**D3.** Assert `trendValue` is derived from `periodPct`, not from `lastWeekComparison.pct`.

**D4.** Assert the returned object contains `comparisonRows: [lastWeekComparison, fourWeekAverageComparison]` — these display rows must still be present.

**D5.** Assert `invertColor: definition.direction === KPI_DIRECTION.LOWER_IS_BETTER` — NOT hardcoded `false`.

**D6.** Read `KPI_CARD_DEFINITIONS`. Spot-check these four entries:
- `freeCpql` → `direction: KPI_DIRECTION.LOWER_IS_BETTER` (cost metric)
- `freeCpgl` → `direction: KPI_DIRECTION.LOWER_IS_BETTER` (cost metric)
- `freeQualified` → `direction: KPI_DIRECTION.HIGHER_IS_BETTER`
- `donationsAmount` → `direction: KPI_DIRECTION.HIGHER_IS_BETTER`

**D7.** Manual trace — verify mentally:
- CPQL: current=$180, previous=$200 → rawDelta=-20, trend='down', invertColor=true
- KPICard: `better = invertColor ? isDown : isUp = true ? true : false = true` → GREEN badge + ↓
- This is the CORRECT result (cost went down = improvement)

**D8.** Assert a multi-line comment exists near the `periodPct`/`trend` lines explaining WHY `lastWeekComparison` is not used for the arrow (the "week" range caused permanent neutral).

---

## CHECK GROUP E — Free group interview dual matcher (DashboardOverview.jsx)

**E1.** Search for `FREE_GROUP_INTERVIEW_LEGACY_URL_TOKEN`. Assert it is defined as:
```js
const FREE_GROUP_INTERVIEW_LEGACY_URL_TOKEN = 'meetings.hubspot.com/andrew-lassise/interview';
```

**E2.** Search for `matchesFreeGroupInterview`. Assert it is defined as a composed OR:
```js
const matchesFreeGroupInterview = (row) => _freeGroupNameMatcher(row) || _freeGroupUrlMatcher(row);
```
NOT a single `createMeetingNameMatcher` call.

**E3.** Assert `_freeGroupNameMatcher` uses `createMeetingNameMatcher(FREE_GROUP_INTERVIEW_MEETING_NAME)` and `_freeGroupUrlMatcher` uses `createTokenMatcher([FREE_GROUP_INTERVIEW_LEGACY_URL_TOKEN])`.

**E4.** Assert `matchesPhoenixInterview` is UNCHANGED — still `createTokenMatcher(PHOENIX_INTERVIEW_MATCH_TOKENS)`.

**E5.** Assert `createTokenMatcher` AND `createMeetingNameMatcher` are both in the import from `dashboardKpiHelpers.js`.

**Business context:** HubSpot filter is "Meeting name contains exactly Sober Founders Intro Meeting" — name matching is primary. The legacy URL token (`meetings.hubspot.com/andrew-lassise/interview`) is a fallback for older records. Last verified week: 3 gross / 2 net meetings matched correctly.

---

## CHECK GROUP F — AttendanceDashboard enrichment gate (AttendanceDashboard.jsx)

**F1.** Search for `Hydrate HubSpot contact enrichment` comment. Read the 8 lines around it.
Assert: The condition before calling `loadHubspotContactEnrichment` is:
```js
if (!hsActivitiesResult.error) {
  void loadHubspotContactEnrichment(...);
}
```
Assert `hsAssocsLoadError` does NOT appear in this condition.

**F2.** Assert a comment near this condition explains: if `hsAssocsLoadError` is set, `hsAssocRows` will be `[]` and the backfill step is safely skipped.

**F3.** Read `loadHubspotContactEnrichment`. Confirm the stale-invocation guard fires BEFORE all `setState` calls (look for `contactEnrichmentInvocationRef.current !== myInvocation`).

**Known limitation (pre-existing, not a regression):** If `hsActivitiesResult.error` is truthy, `contactEnrichmentStatus` stays `'idle'` — it is never set to `'error'`. This was true before the fix as well. Do not treat as a failure.

---

## CHECK GROUP G — Race condition guard (LeadsDashboard.jsx + AttendanceDashboard.jsx)

Verify both files independently.

**G1 — LeadsDashboard.jsx:**
- Assert `useRef` is in the React import (line 1)
- Assert `enrichmentInvocationRef = useRef(0)` is declared INSIDE the component function (not at module level — it must be below the component function declaration)
- Assert `enrichmentInvocationRef.current += 1` comes BEFORE `void loadOptionalHubspotActivityEnrichment(...)`
- Assert the stale check `if (enrichmentInvocationRef.current !== myInvocation) return;` appears BEFORE the first `setState` call inside `loadOptionalHubspotActivityEnrichment`

**G2 — AttendanceDashboard.jsx:**
- Assert `useRef` is in the React import (line 1)
- Assert `contactEnrichmentInvocationRef = useRef(0)` is declared INSIDE the component function
- Assert `contactEnrichmentInvocationRef.current += 1` comes BEFORE `void loadHubspotContactEnrichment(...)`
- Assert the stale check `if (contactEnrichmentInvocationRef.current !== myInvocation) return;` appears BEFORE the first `setState` call inside `loadHubspotContactEnrichment`

**G3 — Race scenario trace (mental verification):**
- Invocation 1: ref=1, myInvocation=1, starts async fetch
- Invocation 2: ref=2, myInvocation=2, starts async fetch
- Invocation 1 resolves: ref.current(2) !== myInvocation(1) → returns, no setState called ✓
- Invocation 2 resolves: ref.current(2) === myInvocation(2) → setState runs ✓

---

## CHECK GROUP H — Test suite

**H1.** Run: `node --test "dashboard/tests/**/*.test.mjs"` from the repo root.
Assert: **22 passed, 0 failed.**

**H2.** Assert these specific test names exist in `dashboard/tests/dashboardKpiHelpers.test.mjs`:
- `legacy URL token still matches free group interview activities`
- `dual matcher catches both name-based and URL-based free group interviews`
- `buildDirectionalComparison returns better tone and null pct when baseline is zero and current is non-zero`
- `buildDirectionalComparison returns neutral when both current and baseline are zero`
- `buildCompletedWeekWindows lastWeek never overlaps current week when today is Monday`

**H3.** Run: `npm --prefix dashboard run lint`
Assert: exits with code 0, no errors.

**H4.** Run: `npm --prefix dashboard run build`
Assert: "built in Xs" with no errors. Pre-existing large chunk warnings on `metaCohortUnitEconPreview` are acceptable — do not fail on those.

---

## CHECK GROUP I — Regression scan

**I1.** Grep `dashboard/src/lib/dashboardKpiHelpers.js` for `return '0'` or `return '0.00'`.
Assert: zero matches.

**I2.** Assert git status is clean (`git status --short` output is empty or only contains `CLAUDE.md` as untracked).

**I3.** Assert git log top 4 commits match (in order):
```
d2e38d9  chore: add CLAUDE.md project instructions
4c6f34a  fix(dashboard): correct trend arrow...
8482d6c  fix(dashboard): correct cost-metric trend arrow...
8b0a64c  fix(helpers): return N/A for non-finite...
```

---

## OPEN ITEMS (require human or production-DB verification — do not fail the audit for these)

**OQ-1 — Interview counting data validation (RESOLVED by product owner)**
HubSpot filter confirmed as "Meeting name contains exactly Sober Founders Intro Meeting". Last week: 3 gross / 2 net (one person attended twice). Name matcher is primary; URL token fallback is safety net for legacy records. No further SQL validation required unless counts diverge unexpectedly.

**OQ-2 — Trend arrow directionality (RESOLVED by product owner)**
Confirmed: higher leads = better (HIGHER_IS_BETTER), lower cost per lead = better (LOWER_IS_BETTER). `invertColor` logic is correct for all cost metrics.

**OQ-3 — Visual trend arrow verification**
After deploying to a running app, select "Week" range on Dashboard Overview. Verify KPI cards now show directional (up/down) trend arrows reflecting week-over-week movement, not all-neutral arrows. This was the core bug being fixed.

---

## Expected Verdict

If all checks in Groups A–I pass: **APPROVE — safe to ship.**

If any check in Groups A, C, D, E, F, G fails: **DO NOT SHIP — regression present.**

If only H3/H4 (lint/build) fail: investigate before shipping.

If only the Known Limitation in F3 is flagged: **APPROVE WITH NOTE** — pre-existing behavior, no regression.
